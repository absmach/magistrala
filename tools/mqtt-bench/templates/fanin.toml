[mqtt]
  [mqtt.broker]
  url = "tcp://localhost:1883"

  [mqtt.message]
  size = 100
  format = "text"
  qos = 2
  retain = true
  payload = ""

  [mqtt.tls]
  mtls = false
  skiptlsver = true
  ca = "ca.crt"

[test]
pubs = 10
subs = 1
count = 100

[log]
quiet = true

[mainflux]
connections_file = "mfconn.toml"